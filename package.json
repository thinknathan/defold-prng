{
	"name": "@types/defold-prng",
	"version": "1.0.1",
	"description": "Types for Defold psuedo-random number generator extension using the Mulberry32 method",
	"license": "CC0-1.0",
	"files": ["**/*.d.ts"],
	"types": "./@types/prng.d.ts",
	"main": "./@types/prng",
	"scripts": {
		"check": "tsc --noEmit",
		"prettier": "prettier \"./**.d.ts\" --write"
	},
	"devDependencies": {
		"prettier": "^3.0.0",
		"typescript": "~5.2.2"
	},
	"peerDependencies": {
		"typescript-to-lua": ">=1.0.0"
	}
}
